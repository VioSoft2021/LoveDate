// Global test setup. Loaded by vitest before any test file.
// Pulls in @testing-library/jest-dom matchers (toBeInTheDocument,
// toHaveAttribute, etc.) so component tests can assert against the DOM.
import '@testing-library/jest-dom/vitest'
